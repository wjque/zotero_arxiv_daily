# 2026-08-01

## Summary

- Completed V01-M2 interest-profile construction and protected export from the active v0.1.0 plan.

## Added

- Added transactional SQLite migration 2 for versioned derived item-digest cache records.
- Added effective-content-hash cache keys that include normalized child signals, deterministic digest reuse, bounded representative-paper projections, and remote-payload budget enforcement.
- Added remote-profile revalidation and GitHub Secret publication through `gh secret set` with payload bytes supplied only on standard input.
- Added tests for cache reuse, command/payload isolation, owner-only export permissions, and unallowlisted field rejection.

## Changed

- Marked V01-M2 completed in the v0.1.0 plan and documented profile cache and protected publication operations in the README.

## Tests and Verification

- Passed formatting, lint, static types, offline unit/integration tests, package build, and artifact-safety inspection.
- GitHub publication was tested with an injected recording runner only; no credential, profile, or Secret was sent to GitHub.

## Compatibility and Migration

- Existing version-1 local SQLite stores upgrade transactionally to schema version 2 by adding only the derived digest-cache table.
- Remote profile schema version remains 1 and uses a positive field allowlist; malformed or expanded exports are rejected before publication.

## Follow-ups

- Implement V01-M3 arXiv discovery and retrieval. Run the opt-in real GitHub publication smoke test only in an approved test repository.

## Local API Doctor Fix

- Updated `doctor` to probe Zotero's `/api/` local data endpoint rather than the separate Connector endpoint. A disabled Local API now produces an actionable configuration diagnostic instead of a false healthy result.
- Added a regression test for the disabled Local API response and verified the corrected probe against the enabled local Zotero instance without retrieving library data.

## V01-M3 arXiv Discovery and Retrieval

### Added

- Added provider-neutral arXiv candidate, canonical identifier/revision, Atom parser, category-expansion, serialized client, retrieval orchestration, and atomic public-metadata state modules.
- Added enforced three-second serialized request spacing, bounded retry/backoff for 429/5xx/network failures, explicit timeout propagation, GMT submission-window queries, one-hop category expansion, overlap-window retrieval, and canonical-ID revision deduplication.
- Added local atomic state that preserves the prior successful checkpoint and seen-ID index until all requested retrieval pages complete successfully.
- Added offline Atom contract and retrieval integration tests for valid/empty/malformed feeds, modern and legacy IDs, revisions, throttling, retry, pagination, deduplication, and checkpoint preservation after failure.

### Tests and Verification

- Passed formatting, lint, static types, and 31 offline tests after completing the arXiv retrieval milestone. No real arXiv request was issued.

### Follow-ups

- Implement V01-M4 local pre-ranking and validated DeepSeek recommendation orchestration. A bounded real arXiv smoke retrieval remains opt-in.

## V01-M4 Recommendation and DeepSeek Pipeline

### Added

- Added transparent local candidate scoring with lexical, category, and recency components; configurable quality threshold; core/adjacent/exploration quotas; and author/topic diversity limits.
- Added provider-neutral structured model-proposal validation that binds every response to an allowlisted arXiv ID and rejects unknown fields, injected URLs, duplicate IDs, invalid quality scores, and overlong text.
- Added recommendation orchestration that excludes known/recent IDs, retains final URL, candidate, selection, and persistence authority locally, and applies model proposals only after validation.
- Added a DeepSeek OpenAI-compatible adapter, bounded batch/token/request enforcement, one bounded transient retry, and all-or-nothing partial-batch policy using injectable transports for offline tests.
- Added atomic local model-proposal cache wired into the recommendation run, plus versioned recommendation-set and non-sensitive run-manifest models containing counts, model, cache usage, token estimate, estimated cost, and duration only.

### Tests and Verification

- Passed offline ranking, adversarial model-output, retry, partial-batch failure, cache reuse, and known-paper exclusion tests; no model API request was issued.

### Follow-ups

- Implement V01-M5 static publishing, authenticated client-side encryption, and browser feedback. A bounded DeepSeek live smoke test remains opt-in after the exposed temporary key is revoked and replaced.

## V01-M5 Static Site, Encryption, and Browser Feedback

### Added

- Added a minimal publishable recommendation schema and strict site-input validation that excludes internal score components and profile data.
- Added a responsive static-site builder with date, topic, category, quota-source, and feedback-state filters; accessible loading, error, no-JavaScript, keyboard-focus, and empty-result states; and arXiv abstract/PDF links.
- Added versioned browser `localStorage` feedback records for `interested`, `not_interested`, `save_for_later`, and `read`, plus one bounded prefilled GitHub Issue URL that retains local feedback until explicit confirmation.
- Added a versioned PBKDF2-SHA256/AES-GCM encryption envelope, Python decryptor, and Web Crypto decryptor. Production output is encrypted unless explicit public output is configured; the passphrase is never written to static output.
- Added the `site build` CLI command and the audited `cryptography` dependency for AES-GCM.

### Tests and Verification

- Added offline encryption round-trip, wrong-passphrase, ciphertext-tampering, encrypted-artifact, public-output, publishable-schema, and CLI build tests.

### Compatibility and Migration

- Introduced version 1 for publishable recommendation data, browser feedback storage, and encryption envelopes. There is no prior M5 data to migrate.

### Follow-ups

- Implement V01-M6 GitHub Actions scheduling, protected state, feedback-issue ingestion, and atomic deployment. Production Pages and feedback-repository visibility still require user configuration and review.

## V01-M6 GitHub Automation and Feedback Loop (In Progress)

### Added

- Added strict versioned parsing for browser-feedback GitHub Issue projections, atomic processed-Issue state, idempotent ingestion, and bounded per-paper short-term ranking adjustments.
- Added the `feedback ingest` CLI command for a JSON projection supplied by an authenticated workflow or offline test harness.
- Made feedback adjustments visible as an explicit local ranking-score component; malformed Issue content cannot advance processed state.
- Added scheduled/manual GitHub Actions orchestration with a non-cancelling production concurrency group, production-environment secrets, same-repository `zotero-feedback` Issue retrieval, protected state persistence, and pinned GitHub Pages artifact deployment.

### Tests and Verification

- Passed offline malformed-payload, duplicate-Issue, atomic-state, predictable-feedback-adjustment, CLI, formatting, lint, and static-type checks for the new feedback slice.
- Confirmed the production Pages source is GitHub Actions and the same-repository feedback label exists. Full package, build, and artifact-safety gates passed before workflow publication; no live model, arXiv, or deployment execution was triggered.

### Follow-ups

- Complete the workflow and deployment slice after the GitHub repository, feedback-Issue visibility, Pages target, and production Secrets have been configured and reviewed.
