# 2026-08-01

## Summary

- Completed V01-M2 interest-profile construction and protected export from the active v0.1.0 plan.

## Added

- Added transactional SQLite migration 2 for versioned derived item-digest cache records.
- Added effective-content-hash cache keys that include normalized child signals, deterministic digest reuse, bounded representative-paper projections, and remote-payload budget enforcement.
- Added remote-profile revalidation and GitHub Secret publication through `gh secret set` with payload bytes supplied only on standard input.
- Added tests for cache reuse, command/payload isolation, owner-only export permissions, and unallowlisted field rejection.

## Changed

- Marked V01-M2 completed in the v0.1.0 plan and documented profile cache and protected publication operations in the README.

## Tests and Verification

- Passed formatting, lint, static types, offline unit/integration tests, package build, and artifact-safety inspection.
- GitHub publication was tested with an injected recording runner only; no credential, profile, or Secret was sent to GitHub.

## Compatibility and Migration

- Existing version-1 local SQLite stores upgrade transactionally to schema version 2 by adding only the derived digest-cache table.
- Remote profile schema version remains 1 and uses a positive field allowlist; malformed or expanded exports are rejected before publication.

## Follow-ups

- Implement V01-M3 arXiv discovery and retrieval. Run the opt-in real GitHub publication smoke test only in an approved test repository.
