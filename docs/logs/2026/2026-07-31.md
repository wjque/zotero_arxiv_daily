# 2026-07-31

## Summary

- Consolidated permanent repository-wide engineering governance into the root `AGENTS.md`.
- Established `docs/plans/` for version and stage plans and `docs/logs/YYYY/` for daily development records.
- Defined the complete proposed v0.1.0 MVP plan from engineering foundation through release hardening.
- Initialized the repository on `main` and configured `git@github.com:wjque/zotero_arxiv_daily.git` as `origin`.

## Added

- Added permanent rules for the development lifecycle, English engineering language, modular structure, reuse, efficiency, testing, security, privacy, and completion criteria.
- Added Semantic Versioning, Git branch, release, file-naming, migration, and refactoring standards.
- Added required formats and maintenance rules for plans and daily logs.
- Added `docs/plans/v0.1.0-initial-mvp.md` with measurable release criteria, architecture, module ownership, eight milestone gates, dependencies, risks, privacy controls, migrations, test strategy, budgets, branch sequence, and rollback procedures.
- Added `.gitignore` rules for interpreter bytecode, build artifacts, virtual environments, tool caches, frontend dependency caches, temporary files, and operating-system metadata; log directories remain tracked.

## Removed

- Removed `CONTRIBUTING.md` and `docs/DEVELOPMENT_SPEC.md`; their permanent engineering rules are now owned exclusively by `AGENTS.md`.
- Removed stage-specific status and milestone content from the permanent rule set.

## Tests and Verification

- Verified that the superseded documents no longer exist.
- Verified that permanent rules are in `AGENTS.md` and the daily record is under `docs/logs/`.
- Verified that engineering governance content is written in English.
- Verified that the v0.1.0 plan contains every plan section required by `AGENTS.md` and uses the required versioned filename.
- Verified that the target GitHub repository is reachable over SSH and has no existing remote refs.
- Verified with `git check-ignore` that generated bytecode, build output, and temporary files are ignored while `docs/logs/` remains tracked.

## Compatibility and Migration

- No application code or persisted application data exists yet, so this governance change requires no runtime migration.

## Follow-ups

- Create the first version plan in `docs/plans/` before beginning implementation.
- Initialize Git and apply branch protection when the GitHub repository is created.
- Status update: the first-plan follow-up above was completed with `docs/plans/v0.1.0-initial-mvp.md`; implementation remains pending plan approval.
