# 2026-07-31

## Summary

- Consolidated permanent repository-wide engineering governance into the root `AGENTS.md`.
- Established `docs/plans/` for version and stage plans and `docs/logs/YYYY/` for daily development records.
- Defined the complete proposed v0.1.0 MVP plan from engineering foundation through release hardening.
- Initialized the repository on `main` and configured `git@github.com:wjque/zotero_arxiv_daily.git` as `origin`.

## Added

- Added permanent rules for the development lifecycle, English engineering language, modular structure, reuse, efficiency, testing, security, privacy, and completion criteria.
- Added Semantic Versioning, Git branch, release, file-naming, migration, and refactoring standards.
- Added required formats and maintenance rules for plans and daily logs.
- Added `docs/plans/v0.1.0-initial-mvp.md` with measurable release criteria, architecture, module ownership, eight milestone gates, dependencies, risks, privacy controls, migrations, test strategy, budgets, branch sequence, and rollback procedures.
- Added `.gitignore` rules for interpreter bytecode, build artifacts, virtual environments, tool caches, frontend dependency caches, temporary files, and operating-system metadata; log directories remain tracked.

## Removed

- Removed `CONTRIBUTING.md` and `docs/DEVELOPMENT_SPEC.md`; their permanent engineering rules are now owned exclusively by `AGENTS.md`.
- Removed stage-specific status and milestone content from the permanent rule set.

## Tests and Verification

- Verified that the superseded documents no longer exist.
- Verified that permanent rules are in `AGENTS.md` and the daily record is under `docs/logs/`.
- Verified that engineering governance content is written in English.
- Verified that the v0.1.0 plan contains every plan section required by `AGENTS.md` and uses the required versioned filename.
- Verified that the target GitHub repository is reachable over SSH and has no existing remote refs.
- Verified with `git check-ignore` that generated bytecode, build output, and temporary files are ignored while `docs/logs/` remains tracked.

## Compatibility and Migration

- No application code or persisted application data exists yet, so this governance change requires no runtime migration.

## Follow-ups

- Create the first version plan in `docs/plans/` before beginning implementation.
- Initialize Git and apply branch protection when the GitHub repository is created.
- Status update: the first-plan follow-up above was completed with `docs/plans/v0.1.0-initial-mvp.md`; implementation remains pending plan approval.

## V01-M0 Engineering Foundation

### Added

- Added the v0.1.0 Python package, authoritative version source, `uv.lock`, MIT license, README, safe environment-variable example, and ADR directory guidance.
- Added strict configuration loading with defaults, file, environment, and CLI precedence; local Zotero endpoint validation; stable exit codes; structured JSON logging; injectable UTC clock; and version-1 profile, recommendation, feedback, and run-manifest boundary models.
- Added `zotero-arxiv-daily doctor`, which diagnoses the local Zotero connector and DeepSeek, GitHub, and Pages configuration independently without revealing credentials.
- Added offline CI with pinned GitHub-owned actions, least-privilege permissions, formatting, lint, type, tests, package build, and artifact-safety checks.
- Added synthetic unit coverage for configuration precedence and failures, diagnostics and exit policy, CLI output safety, timestamp and URL validation.

### Changed

- Activated `docs/plans/v0.1.0-initial-mvp.md` and recorded V01-M0 as complete. No durable architectural baseline changed, so no ADR was needed.
- Extended `.gitignore` to exclude local credential files and future local runtime data while retaining the committed environment-variable template.

### Tests and Verification

- Passed `ruff format --check .`, `ruff check .`, `mypy`, and `pytest` (9 passed) in the locked development environment.
- Passed `uv build`, `scripts/check_artifacts.py`, and `git diff --check`.
- Exercised `doctor --skip-zotero-check --format json`; it returned the documented configuration exit code and independently reported missing credentials without printing values.
- Exercised the real local connector probe without reading library data; Zotero Local API responded successfully, while missing remote credentials remained separate actionable diagnostics.

### Compatibility and Migration

- Introduced initial schema version 1 constants only; no pre-existing application state or migration is required.
- No real Zotero content, credentials, exports, prompts, or generated recommendation data was used or added.

### Follow-ups

- Implement V01-M1 local Zotero synchronization and V01-M3 arXiv discovery against the V01-M0 boundaries.

## V01-M1 Local Zotero Synchronization

### Added

- Added the `zotero` module with a serialized, timeout-bounded Local API adapter that records the advertised API version; strict item and collection normalization; safe HTML-to-text processing; and separate normalized note, highlight, and annotation-comment fields.
- Added a local-only SQLite version-1 store for item versions, normalized content hashes, collections, memberships, parent relationships, and the last successful library version.
- Added `zotero-arxiv-daily profile sync`, which uses a full first fetch and `since`-based incremental fetches thereafter. Output contains counts only.
- Added synthetic unit and integration tests for manual/automatic tags, sanitization, non-seed child types, parent ownership, unchanged reruns, edits, collection moves, and deletion.
- Narrowed the Zotero-export artifact scan to serialized data formats after a synthetic Python test input caused a false positive; credential checks remain applied to all tracked text.

### Changed

- Marked V01-M1 complete in the v0.1.0 plan and documented the implemented local-sync operation in the README.

### Tests and Verification

- Passed formatting, lint, static types, and 15 offline tests covering the new Zotero boundaries and existing foundation behavior.

### Compatibility and Migration

- Introduced the first local SQLite schema with a transactional initial creation path. There is no prior application database to migrate.
- The default `runtime/` state and user-selected local database paths are ignored by Git; no real Zotero records, notes, annotations, or exports were used in test fixtures.

### Follow-ups

- Implement V01-M2 interest-profile generation and V01-M3 arXiv retrieval. A real-library count-only smoke sync remains an explicit user-run operation.
