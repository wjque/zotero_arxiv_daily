# 2026-08-03

## Summary

- Refined the active v0.2.0 requirements after reviewing the role of the user's preliminary curated
  paper collection and the operational cost and stability of feedback-driven learning. V020-M1
  remains pending and no runtime implementation started.

## Changed

- Defined the curated corpus as an evolving local judgment source rather than a fixed dataset.
  Additions, corrections, reason changes, and explicit unlabeling will retain lineage, while each
  reproducible evaluation pins an immutable corpus revision, digest, cutoff, and split identities.
- Changed personalized screening-criteria activation from a possible daily update to a guarded
  weekly cadence. Feedback events may still be ingested on scheduled runs, but daily recommendations
  use the last successful weekly aggregate snapshot.
- Added requirements for configurable weekly eligibility in `Asia/Shanghai`, deterministic event
  cutoffs, minimum-sample no-ops, stable-anchor and rolling evaluation windows, accumulated-event
  recovery after missed or failed runs, atomic activation, versioned rollback, and injected-clock
  boundary tests.
- Kept automatic weekly learning limited to bounded derived preference aggregates. Prompt, rubric,
  security, exploration, and production-weight changes remain separately versioned and explicitly
  activated.

## Compatibility and Migration

- This is a documentation-only requirement update. It changes no schema, configuration, runtime
  state, deployment behavior, model cost, trust boundary, or private corpus content. Expected weekly
  activation state and correction lineage remain future V020-M1/V020-M3 implementation work.

## Tests and Verification

- Verified the documentation update with `.venv/bin/ruff format --check .`,
  `.venv/bin/ruff check .`, `.venv/bin/mypy`, `.venv/bin/pytest`,
  `/root/.local/bin/uv build`, `scripts/check_artifacts.py`, and `git diff --check`: 85 files were
  formatted, lint passed, strict typing reported no issues in 72 source files, all 94 offline tests
  passed, the v0.1.2 sdist and wheel built, and artifact safety passed.

## Follow-ups

- Confirm the Zotero collection mapping and initial reason-code taxonomy before explicitly starting
  V020-M1.

## V020-M1 Implementation

- Completed V020-M1 for v0.2.0 on branch `feature/v0.2-evaluation-corpus-foundation`. Added the
  local-only `evaluation/` module with an append-only curated-corpus ledger, correction lineage,
  explicit unlabeling, deterministic corpus snapshots, stable-anchor/rolling/temporal/pairwise
  splits, ranking metrics, provisional-result handling, comparison reports, and write-once
  evaluation snapshot files.
- Added `corpus list-collections` and `corpus import-zotero` commands. The importer accepts explicit
  positive and hard-negative collection keys, recognizes DOI identity and an opt-in
  `ranking-paper-id:arxiv:<id>` tag, and reads optional `ranking-reason:<code>` tags. The real
  corpus remains in ignored owner-only `runtime/curated-corpus.json` state.

### Changed

- Updated the v0.2.0 plan and README with the completed V020-M1 scope and local corpus operation.
  No production ranker, feedback activation cadence, protected remote profile, model call, or
  browser artifact changed.

### Tests and Verification

- Added synthetic unit coverage for corpus correction/unlabeling, Zotero import idempotency,
  canonical identity handling, immutable snapshots, temporal/anchor/rolling/pairwise splits,
  metrics, and insufficient data. Full checks passed: `.venv/bin/ruff format --check .`,
  `.venv/bin/ruff check .`, `.venv/bin/mypy`, `.venv/bin/pytest` (102 tests),
  `/root/.local/bin/uv build`, `python scripts/check_artifacts.py`, and `git diff --check`.
