# 2026-08-04

## Summary

- Continued the active v0.2.0 personalized-ranking plan without changing production activation,
  publishing runtime data, or adding a new approved trust boundary.

## Added

- Added applicability-aware normalized ranking feature groups, immutable local weight-set registry
  definitions, reversible active pointers, score contributions, and source-aware exploration reserve.
- Added a local `evaluate snapshot` / `evaluate shadow` workflow. It freezes corpus inputs, compares
  the frozen v0.1.2 scorer with the active local weight set, reports aggregate Recall/Precision/NDCG,
  negative-label rate, calibration/diversity/coverage metrics, and feature-group ablations, and writes
  owner-only ignored reports without changing runtime recommendation state.
- Added a gated two-stage LLM recommendation path. `judge-v1` supplies bounded quality and uncertainty
  features to local selection; `explain-v1` produces summary/reason/limitation text only for final
  selections. Judge and explanation caches remain separate and validated before they are written.
- Added publishable-site schema v4 with an optional limitation note and retained exact v1-v3 readers.
- Added lockfile-backed Playwright browser tests for desktop and mobile Chromium. They decrypt a
  backend-generated AES-GCM schema-v4 fixture, render quality/uncertainty/limitation fields, and
  confirm unsupported schemas fail closed; CI now runs this gate.

## Changed

- Corrected normalized coarse ranking to classify core, adjacent, and exploration candidates correctly.
  Missing or inapplicable evidence is excluded from score normalization rather than interpreted as zero.
- The daily workflow persists the local ranking weight registry after a successful deployment. LLM
  refinement and its categorical preference context remain disabled by default; the latter needs a
  field-level trust-boundary approval before it can be enabled.
- Updated the v0.2.0 plan after the arXiv outage and release-readiness review. Corpus size now controls
  uncertainty reporting rather than a fixed release gate, while automatic tuning remains disabled;
  the evolving corpus is evaluated through immutable pinned snapshots.
- Required LLM judging and explanation to receive complete normalized titles and abstracts, with
  deterministic whole-record batching, bounded retries/cost, and explicit oversized-record handling
  replacing character truncation.
- Expanded V020-M8 release hardening to cover browser schema v4 execution, arXiv retry spacing and
  validated-pool degradation, strict state restoration, deploy/state reconciliation, explicit ranker
  rollback, and confirmed ranking/cache/metric correctness defects.
- Added two-phase workflow state reconciliation. A prepared deployment persists the generated batch,
  candidate state, caches, and history candidate before Pages deployment; a later run checks the prior
  Deploy step and records impressions/history exactly once when the state push was interrupted.
- Exposed bounded LLM batch, request, retry, byte, and output-token settings through `AppConfig` and
  `.env.example`; DeepSeek response bodies now have a 2 MiB adapter limit. Sparse evaluation warnings
  remain separate from the automatic-tuning eligibility flag, while explicit activation remains a
  manual action.
- Corrected symmetric hyphen/space facet matching and retained bounded arXiv degraded diagnostics
  without copying provider response bodies into local state.
- Propagated candidate-pool degradation and its source checkpoint into the private run manifest and
  static-site freshness metadata; the browser batch status now labels a recent validated fallback.
- Bumped the authoritative application version to `0.2.0`, documented the local release candidate in
  README and CHANGELOG, and marked completed browser, deployment-reconciliation, and normalized-ranker
  plan items accurately while retaining production evidence milestones as Active.
- Updated the repository engineering standard so newly written plans are immutable unless the user
  explicitly authorizes a specific edit. Future plans contain only stage goals, milestone behavior,
  and measurable acceptance methods; implementation design, progress, and outcomes belong in code,
  tests, ADRs, logs, changelogs, or release records.
- Required agents to reassess implementation choices throughout development and debugging, prefer a
  coherent refactor over patch-on-patch changes when it materially improves efficiency or simplicity,
  remove superseded paths, and support performance claims with measurements.
- Added manual production-canary inputs for the v0.2 judge/explain path and its separately controlled
  categorical preference context. Scheduled runs keep both inputs disabled; candidate run
  `30880747238` enabled refinement while leaving preference context disabled.
- Replaced the production environment's protected profile Secret with a validated schema-v4 profile
  built from source-library version `4869` (`30` topics, `3` core categories, `3` adjacent categories,
  and `9` bounded preference facets).

## Tests and Verification

- Added unit coverage for source attribution, unknown evidence, immutable weight-set rollback,
  final-only explanation generation and cache reuse, schema-v4 limitation migration, and provisional
  shadow evaluation/ablations.
- Passed `./.venv/bin/ruff format --check .`, `./.venv/bin/ruff check src tests`, strict mypy, and
  `./.venv/bin/pytest -q -p no:cacheprovider` (161 tests). `./.venv/bin/python
  scripts/check_artifacts.py` passed, the workflow YAML parsed successfully, and
  `UV_CACHE_DIR=/tmp/zad-uv-cache /root/.local/bin/uv build --out-dir /tmp/zad-v020-dist-final`
  built the sdist and wheel. `git diff --check` passed.
- Passed `npm run test:e2e` with four Playwright cases across desktop and mobile Chromium. The tests
  used only synthetic public metadata and a synthetic passphrase under ignored temporary storage.
- Reviewed the updated plan for obsolete 40-label release gates, truncated-input requirements,
  milestone consistency, rollback coverage, and explicit ownership of the newly identified release
  blockers; the implementation and verification results are recorded above.
- Added site freshness and bounded-diagnostic regression coverage; targeted site, CLI, and manifest
  tests passed after the propagation change.
- Clarified the active plan outcome: implementation foundations are complete, while real-corpus,
  approval, canary, rollback, and formal release gates remain pending.
- Rebuilt the derived profile v4 from the local corpus using a temporary writable SQLite copy
  (`30 topics, 3 core categories, 3 adjacent categories, 9 facets`); the checked `runtime/` mount
  rejected atomic writes, so the formal runtime profile was left unchanged.
- Pinned evaluation snapshot `eval-20544643722817f18e827198` with 32 labels. The checked runtime had
  no candidate state; the protected 998-paper state snapshot was also evaluated and had zero DOI-label
  overlap, Recall@60 `0.0`, and a blocked result. One bounded public arXiv retrieval attempt exhausted
  on HTTP 429 without creating a newer candidate pool.
- Added an explicit candidate-label overlap metric and zero-overlap warning to prevent holdout label
  counts from being reported as candidate coverage; the updated shadow report now exposes the
  provisional zero-overlap condition directly.
- Added exact DOI aliases to offline ranked-paper projections, allowing DOI corpus labels to match
  candidates with verified DOI metadata while rejecting identity conflicts and avoiding fuzzy search.
- Reviewed `AGENTS.md` for conflicting plan-update, status-maintenance, implementation-detail, and
  patch-on-patch requirements; `git diff --check` passed. This governance-only change did not require
  runtime tests, and no existing plan content was changed as part of the rules update.
- Pushed candidate commit `e8008d9` to `main`. CI run `30880640786` passed formatting, lint, strict
  types, 161 Python tests, four desktop/mobile Chromium tests, package build, and artifact inspection.
- Production candidate run `30880747238` completed in 1 minute 42 seconds. It retrieved a fresh
  non-degraded 1,000-paper pool, selected 20 recommendations through three model requests, added 40
  judge and 20 explanation cache entries, deployed encrypted schema-v4 Pages, recorded 20 impressions,
  and persisted a `deployed` receipt and final state without pending files.
- Verified `https://wjque.github.io/zotero_arxiv_daily/` returns HTTP 200, the live configuration uses
  encrypted output with no degraded candidate marker, and the AES-GCM/PBKDF2 envelope contains no
  inspected plaintext patterns.
- Replayed pinned snapshot `eval-20544643722817f18e827198` against the fresh production candidate
  state. Only 16 of 1,000 candidates carried a DOI and exact candidate-label overlap remained zero;
  the coarse-v1 report is provisional and ineligible for activation.
- Confirmed a production privacy defect: the repository is public and anonymous raw URLs return HTTP
  200 for `state`-branch feedback state, recommendation history, and model proposal cache. Branch
  protection does not make branch contents confidential.

## Compatibility and Migration

- Publishable schema v4 carries an optional bounded limitation. v1-v3 payload readers default it to
  unavailable. The local weight registry is newly created ignored owner-only state and defaults to
  `coarse-v1`; it has no impact on existing deployments until a new run persists it.
- The application package now identifies as `0.2.0`; all v0.1.x persisted profile, feedback,
  candidate, recommendation, and publishable inputs retain explicit compatibility paths.
- The production candidate migrated the feedback state from schema v1 to schema v2 and the protected
  profile from schema v3 to schema v4. The published recommendation payload remains encrypted and
  backward-readable, but state-branch confidentiality requires a separate migration before release.

## Follow-ups

- The real local corpus currently contains 32 independent labels. Its metrics must expose overlap and
  uncertainty, but the count does not block an explicitly approved initial canary. A pinned snapshot,
  approved preference-context field audit, browser execution coverage, production canary, rollback
  rehearsal, and release approval remain required. Post-deployment reconciliation is implemented and
  covered by workflow assertions, but has not yet been exercised against a live failed state push.
- Treat public production state as the highest-priority v0.2 release blocker. Move feedback events,
  recommendation history, and model caches to confidential durable storage or encrypt them with an
  explicit key/migration/rollback design; then remove public copies and address retained Git history.
- Persist the privacy-safe run manifest as a restricted artifact or encrypted state. The successful
  candidate exposed request count but lost token, cost, latency, retry, and cache-hit details after the
  runner exited, so the v0.1.2 efficiency comparison cannot yet be audited.
- Persist an explicit production ranking pointer. The candidate used the built-in `coarse-v1` fallback,
  while `ranking-weights.json` still records `active_version: null`; activation and rollback should not
  depend on an implicit fallback.
- Build a candidate-overlapping held-out corpus before approving ranking quality. The current 32-label
  snapshot and fresh 1,000-paper pool still have zero exact overlap, and fuzzy identity matching must
  remain prohibited.
- Keep LLM preference context disabled until its field audit is approved and the public-cache exposure
  is eliminated. Complete a live state-push reconciliation rehearsal and explicit ranker rollback
  before creating the immutable `v0.2.0` tag or GitHub Release.
