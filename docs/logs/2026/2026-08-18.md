# 2026-08-18

## Summary

- Approved and froze the v0.2.1 release after operator review of the protected 20-paper production
  canary.

## Changed

- Finalized the v0.2.1 changelog date and changed the README status from release candidate to
  released behavior.
- Retained the approved `quality-profile-317dcd78877633a5`, `quality-reference-policy-v2`,
  `quality-first-v1` ranking weights, and the existing encrypted workflow state without migration.
- Published the immutable annotated `v0.2.1` tag at release commit `d284f3d` and created the public
  GitHub Release with the verified source distribution and wheel.
- Started v0.3.0 implementation on `feature/v0.3-outcome-feedback` and changed the authoritative
  development version to `0.3.0`.
- Replaced the one-action browser state with independent pre-reading, reading, and post-reading
  stages. Submitted state is retained, corrections remain append-only, and only pending changes are
  exported.
- Bound new feedback to the exact successful publication batch. Legacy pending browser feedback is
  retained as unattributed instead of being guessed into the current batch.

## Tests and Verification

- Confirmed local `main` and `origin/main` were both at `3321b52` before the release freeze and that
  no `v0.2.1` tag existed after fetching remote tags.
- Passed Ruff formatting and lint, strict mypy over 116 source files, and 225 offline unit,
  contract, and integration tests.
- Passed four Playwright desktop/mobile Chromium tests, parsed every workflow YAML file, built the
  `0.2.1` source distribution and wheel, and passed artifact and credential-pattern inspection.
- Confirmed the operator reviewed and approved the decrypted 20-paper recommendation batch from
  production canary run `31502407895` and the metadata-validation boundary from run `31503190721`.
- Verified the GitHub Release is neither a draft nor a prerelease and that both package assets are in
  the uploaded state. The release notes record SHA-256 checksums for both artifacts.
- Passed 51 focused feedback, ledger, CLI, and site tests plus eight Playwright desktop/mobile tests
  for the v0.3 outcome slice. The browser tests cover read-before-outcome enforcement, batch-scoped
  v2 export, retained submitted state, legacy browser migration, and schema rejection.
- Passed the complete v0.3 slice gate: Ruff formatting and lint over 145 files, strict mypy over 116
  source files, 249 offline tests, three workflow YAML parses, and local `0.3.0` source-distribution
  and wheel builds.
- Passed tracked-file credential and private-content inspection plus static-site raw, gzip, and total
  size budgets against the generated encrypted browser fixture. The pre-existing ignored runtime site
  was restored unchanged after verification.

## Compatibility and Migration

- Existing publishable schema v1-v4 readers, encrypted protected state, and the explicit v0.1.2
  ranking rollback remain supported. v0.2.1 requires no additional operator migration.
- Feedback Issue schema v1 and persisted ledger schemas remain readable. Browser feedback schema v2
  adds bounded batch-scoped staged actions without changing the encrypted ledger schema or applying
  feedback to production ranking.

## Added

- Added the immutable v0.3.0 outcome-optimized recommendation plan. Its primary metric is explicit
  worthwhile reads per published batch, with distinct post-reading outcomes, bounded cross-category
  coverage, safe exploration, outcome-policy evidence, and explicit activation approval.
- Added explicit `worthwhile` and `not_worthwhile` post-reading outcomes, deterministic delayed and
  correction handling, and aggregate-only `feedback report` batch metrics.
- Added ADR-0008 for the batch-scoped explicit-outcome protocol and its legacy rollback behavior.
