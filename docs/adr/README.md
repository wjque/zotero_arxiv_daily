# Architecture Decision Records

This directory holds durable architecture and trust-boundary decisions. Use one
numbered Markdown record per decision once a decision changes or confirms an active
plan baseline. Daily activity belongs in `docs/logs/`, and release execution belongs
in `docs/plans/`.
